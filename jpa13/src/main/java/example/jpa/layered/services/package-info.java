/**
 * Service layer.
 * 
 * Encapsulates business logic. Defines transaction boundaries.
 * Uses data access layer for database operations, which makes it a higher
 * level layer.
 */
package example.jpa.layered.services;
