/**
 * JPA entities used to demonstrate ID generation strategies.
 *
 * Note that JPA entity names are per-persistent unit and therefore
 * it doesn't make sense to spread them over Java packages.
 */
package example.jpa.idgen.entities;
